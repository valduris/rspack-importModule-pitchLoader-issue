{
  "name": "rspack-importmodule-pitchloader-issue",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal repro: this.importModule() + a pitch loader targeting a type: \"asset\" module resolves to the pitch loader's own generated source under rspack instead of the real target's content - only under rspack, not webpack",
  "type": "module",
  "scripts": {
    "repro": "node build.mjs"
  },
  "dependencies": {
    "@rspack/core": "2.2.3",
    "webpack": "5.108.4"
  }
}
